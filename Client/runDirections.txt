in folder Client

COMPILE

g++ -std=c++20 -Wall -o client client.cpp

RUN

./client hostname port

./client 127.0.0.1 5400

*** hostname needs to be 127.0.0.1 for now may need to fix to resolve csx1, csx2, csx3